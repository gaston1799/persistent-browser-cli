{
  "name": "persistent-browser-cli",
  "version": "0.1.0",
  "description": "A Windows-focused CLI for driving a persistent Chrome profile over CDP and Playwright.",
  "type": "commonjs",
  "bin": {
    "pbc": "./cli.js",
    "persistent-browser-cli": "./cli.js"
  },
  "scripts": {
    "help": "node cli.js --help"
  },
  "keywords": [
    "chrome",
    "playwright",
    "cdp",
    "browser",
    "persistent-profile",
    "automation"
  ],
  "license": "MIT",
  "dependencies": {
    "@playwright/cli": "^0.1.5",
    "playwright-core": "^1.59.1"
  }
}
